<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plaintext HTTP only to the local dev backend (Env.apiBaseUrl's
  default) — 10.0.2.2 is the Android emulator's alias for the host machine.
  A prod build's HTTPS base URL is unaffected by this and stays locked
  down by Android's default (cleartext-disabled) policy.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
